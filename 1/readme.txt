#Write a program to implement an address book with options given below:
#a) Create address book. b) View address book. c) Insert a record. d) Delete a record.
#e) Modify a record. f) Exit.




To change the file permessions i.e to allow the .sh file to execute we need to grant premessions to the file that can be achieved by the following command
1) chmod +x file_path
2) ./file_name.sh to run the shell script